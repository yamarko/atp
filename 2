#include <stdio.h>

int main()
{
    int a = 0, b = 0;
    scanf("%i%i", &a, &b);
    for (int i = a ; b >= i ; i++)
    {
        printf("%i\n", i);
    }
    printf("Kilkist= %i\n", (b - a - 1));
}
